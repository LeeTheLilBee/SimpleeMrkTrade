# Managed Staging Service Shell

This directory is the deployable repository-side shell for one Tower-fronted managed Python staging web service. The managed WSGI target is `web.managed_staging:app`. Tower remains the only browser front door. Observatory public ingress, production deployment, database creation, object storage creation, DNS changes, broker submission, real capital movement, and Live Auto remain locked.
