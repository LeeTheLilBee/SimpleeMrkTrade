
# OBUX066–070 — Canonical Beta Entry + V27 Retirement

## OBUX066 — Canonical Tower product entry

The historical Tower Observatory walkthrough remains available as an
internal proof/evidence surface.

It is no longer the normal product doorway.

Normal Tower owner launch now resolves to:

`/ob/dashboard`

The route remains protected by the existing Tower OB web boundary:

- active Tower owner session required
- active Tower step-up required for normal OB rooms
- unknown `/ob/*` paths fail closed

No security weakening is introduced.

## OBUX067 — Real arrival sequence

Dashboard release metadata advances to:

- build: `OBUX066-070`
- SOP: `beta-sop-v2`
- What Changed: `obux066-070-v1`

This causes one new version-aware arrival for existing beta browsers.

Normal arrival remains:

Tower
→ Dashboard privacy cover
→ versioned SOP if required
→ Soulaana optional check-in
→ safe resume if available
→ optional first-session guide
→ Dashboard

A deliberate owner/testing replay is available at:

`/ob/dashboard?ob_arrival=fresh`

The compact OB drawer exposes the replay action.

Fresh replay shows:

- SOP
- Soulaana check-in
- guide offer

and then removes the replay query marker from the browser URL.

## OBUX068 — V27 retirement

`ob_atmosphere.css` remains in the repository for historical room geometry,
legacy/proof compatibility, and regression coverage.

Its old OBUX027 room-weather palette is no longer authoritative on modern
themed Observatory product surfaces.

The selected theme now owns:

- visible sky canvas
- weather glow
- horizon
- stars
- subtle grid

Themes remain:

- Obsidian Plum
- Velvet Night
- Eclipse Gold

## Locked boundaries

This pack does not add:

- broker submission
- real capital movement
- automatic contract choice
- automatic execution
- Live Auto authorization

Mission accounts remain owner-only.
